import { Box, ProgressBar } from 'cabana-react';
import { StatefulExample, Slider } from './helpers.js';

<a id="ProgressBar"></a>

# ProgressBar
Note: the sliders included below are to simulate the changing progress of the `ProgressBar`.
<Box width={1 / 3}>
    <StatefulExample innerBg="primary" />
    <StatefulExample innerBg="tertiary" borderRadius="large" />
    <StatefulExample innerBg="success" color="white" p={1} fontSize="body" borderRadius="large" />
    <StatefulExample innerBg="primary" p={1} borderRadius="none" />
    <StatefulExample innerBg="primary" fontSize="uber" />
    <StatefulExample innerBg="primary" showNumber={false} minHeight={20} />
</Box>